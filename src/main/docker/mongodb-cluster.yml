version: '2'
services:
    financeiro-mongodb:
        container_name: financeiro-mongodb
        image: mongo:3.3.9
        ports:
            - "27017:27017"
        command: mongos --configdb financeiro-mongodb-config
    financeiro-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    financeiro-mongodb-config:
        image: mongo:mongo:3.3.9
        container_name: financeiro-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
